<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="apurupa.iiitd.com.energyapp.AcceptRequest">

    <LinearLayout
        android:id="@+id/AcceptRequestLinearView"
        android:layout_width="300dp"
        android:layout_height="100dp"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="150dp"
        android:layout_marginLeft="30dp"
        android:orientation="horizontal"
        android:weightSum="3"

        >
        <TextView
            android:id="@+id/AcceptRequestTextView"
            android:layout_width="300dp"
            android:layout_height="match_parent"
            android:padding="15dp"
            android:textColor="@color/bgBottomNavigation"
            android:text="@string/Status"
            android:background="@drawable/rounded_corner"
            android:textAlignment="center"

            android:textAppearance="?android:attr/textAppearanceMedium" />
    </LinearLayout>

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Accept"
        android:id="@+id/acceptButton"
        android:background="@android:color/holo_green_dark"
        android:textColor="@color/bgBottomNavigation"
        android:layout_below="@+id/AcceptRequestLinearView"
        android:layout_alignParentStart="true"
        android:layout_marginTop="57dp" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Ignore"
        android:id="@+id/ignoreButton"
        android:background="@android:color/holo_red_dark"
        android:textColor="@color/bgBottomNavigation"
        android:layout_alignTop="@+id/acceptButton"
        android:layout_alignParentEnd="true" />


</RelativeLayout>
