<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="apurupa.iiitd.com.energyapp.StatusPendingActivity"
    android:background="@color/bgBottomNavigation">

    <LinearLayout
        android:id="@+id/statusPendingLinearView"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="180dp"
        android:layout_marginLeft="70dp"
        android:orientation="horizontal"
        android:weightSum="3"

        >
        <TextView
            android:id="@+id/statusPendingTextView"
            android:layout_width="200dp"
            android:layout_height="match_parent"
            android:padding="15dp"
            android:textColor="@color/bgBottomNavigation"
            android:text="@string/Status"
            android:background="@drawable/rounded_corner"

            android:textAppearance="?android:attr/textAppearanceMedium" />
    </LinearLayout>

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Signout"
        android:id="@+id/SignOutButton"
        android:layout_below="@+id/statusPendingLinearView"
        android:layout_centerHorizontal="true"
        android:background="@drawable/rounded_corner"
        android:textColor="@color/bgBottomNavigation"
        android:layout_marginTop="78dp" />

</RelativeLayout>
